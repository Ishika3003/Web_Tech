#sum of even number

def sum_even(num):
    if(num==2):
        return 2
    else:
        if(num%2==0):
            return num + sum_even(num-2)
        else:
            return (num-1) + sum_even(num-3)
print(sum_even(7))

#output 12
